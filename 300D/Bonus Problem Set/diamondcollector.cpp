#include <bits/stdc++.h>
#define FASTIO ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
using namespace std;

int main() {
    int n, k;
    cin >> n >> k;
    vector<int>diamonds(n+1, 0);
    for (int i=0; i<n; i++) {
        cin >> diamonds[i];
    }
}
#include <bits/stdc++.h>
#define FASTIO ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
using namespace std;

int arr[2501][2501];

int combo(int x1, int y1, int x2, int y2) {
    
}

int main() {FASTIO;
    int n, x, y;
    cin >> n;

    while (n--) {
        cin >> x >> y;
        arr[x][y]++;
    }
}
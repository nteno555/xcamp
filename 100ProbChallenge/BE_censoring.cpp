#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
    string word, text;
    cin >> text >> word;

    for (int i=0; i<(text.length()-word.length()); i++) {
        if ()
    }
}